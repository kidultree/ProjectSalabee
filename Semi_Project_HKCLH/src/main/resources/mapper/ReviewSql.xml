<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="data.mapper.ReviewMapperInter">

<!-- INSERT -->
	<insert id="insertReview" parameterType="review">
		insert into review
		(rnum, pnum, rrate, rtitle, rname, mid, rcontent, rphoto,
		rdate) values
		(
		#{rnum}, #{pnum}, #{rrate}, #{rtitle}, #{rname}, #{mid}, #{rcontent},
		#{rphoto}, #{rdate}
		)
	</insert>


<!-- 수정 -->
	<select id="getReview" parameterType="int" resultType="review">
		select *
		from review where rnum = #{rnum}
	</select>


<!-- 삭제 -->
	<delete id="deleteReview" parameterType="int">
		delete from review where
		rnum=#{rnum}
	</delete>

<!-- LIST -->
	<select id="getReviewList" resultType="review">
		select * from review
	</select>
	
	</mapper>