<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="data.mapper.NoticeMapperInter">
<!-- Notice -->
<insert id="insertNotice" parameterType="notice">
insert into notice (nTitle, nName, nContent, nImg, nDate) values (
#{nTitle}, #{nName}, #{nContent}, #{nImg}, now()}
</insert>
<!-- 리스트 -->
<select id="getNoticeList" parameterType="Map" resultType="product">
select * from notice order by nNum desc limit #{start}, #{perpage}
</select>

<!-- 수정 -->
<select id="getNotice" parameterType="int" resultType="product">
select * from notice where nNum = #{nNum}
</select>

<!-- 삭제 -->
<delete id="deleteNotice" parameterType="int">
delete from notice where nNum=#{nNum}
</delete>

<select id="getTotalNoticeCount" resultType="int">
select count(*) from notice
</select>

</mapper>