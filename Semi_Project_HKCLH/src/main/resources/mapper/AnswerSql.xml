<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="data.mapper.AnswerMapperInter">
	
	<insert id="insertAnswer" parameterType="answer">
		insert into answer (aansnum, qnum, mname, mid, acontent, adate) values (#{aansnum}, #{qnum},
		#{mname}, #{mid}, #{acontent}, now())
	</insert>
	
	
	<select id="getAnswerList" parameterType="int" resultType="answer">
		select * from answer where qnum=#{qnum} order by aansnum desc
	</select>
	
	
	<delete id="deleteAnswer">
		delete from answer where aansnum=#{aansnum}
	</delete>

</mapper>