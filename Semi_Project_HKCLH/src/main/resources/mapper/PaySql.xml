<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="data.mapper.PayMapperInter">
	<insert id="insertPay" parameterType="pay">
		insert into member (mid,pydate,pyname,pyphoto,pyopyion,pyquantity,pydelivery)
		 values (#{mid},#now(),#{pyname},#{pyphoto},#{pyopyion},#{pyquantity},#{pydelivery})
	</insert>
</mapper>